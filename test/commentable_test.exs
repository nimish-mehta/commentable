defmodule CommentableTest do
  use ExUnit.Case

  @moduledoc """
  guess table_name based on module and adjusting all glue
  adds comment_with helper to add association and comments_changeset
  adds acts_as_comment helper to insert needed fields into schema to persist and access
  adds comment_columns to help with migrations
  """
end
